# Gas Town WebUI 环境变量配置示例
# 复制此文件为 /etc/gastown.env 并填入真实的配置
#
# 使用方法：
#   sudo cp deploy/gastown.env.example /etc/gastown.env
#   sudo nano /etc/gastown.env  # 填入真实的 Token
#   sudo chmod 600 /etc/gastown.env  # 设置权限，仅 root 可读
#   sudo systemctl restart gastown-web

# ============================================================================
# 认证配置（必需）
# ============================================================================

# WebUI 认证 Token
# 生成方法: openssl rand -hex 32
# 这是访问 WebUI 时在 /login 页面输入的密码
GT_WEB_AUTH_TOKEN=your-secure-token-here-replace-me

# 允许非 localhost 访问（必须同时设置 TOKEN）
# 如果你有反向代理（nginx/Caddy），需要设置为 1
GT_WEB_ALLOW_REMOTE=1

# ============================================================================
# Git 同步配置（可选）
# ============================================================================

# Git 仓库根目录（如果需要 WebUI 修改后自动提交）
# GT_WEB_GIT_REPO_ROOT=/home/shisui/gt

# Git 作者信息
# GT_WEB_GIT_AUTHOR_NAME=Gas Town WebUI
# GT_WEB_GIT_AUTHOR_EMAIL=webui@gastown.local

# Git 提交者信息（如果不设置，会使用 AUTHOR）
# GT_WEB_GIT_COMMITTER_NAME=Gas Town WebUI
# GT_WEB_GIT_COMMITTER_EMAIL=webui@gastown.local

# 保存后执行的 Hook 脚本
# GT_WEB_POST_SAVE_HOOK=/home/shisui/gt/scripts/webui-git-sync.sh

# ============================================================================
# Dashboard 配置（可选）
# ============================================================================

# Dashboard 显示的邮箱身份
# GT_DASHBOARD_MAIL_IDENTITY=overseer

# ============================================================================
# 其他配置
# ============================================================================

# Town 根目录（通常自动检测，无需设置）
# GT_ROOT=/home/shisui/gt
